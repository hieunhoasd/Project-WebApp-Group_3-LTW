version: '3.8'

services:
  mysql_db:
    image: mysql:8.0
    container_name: ltw_mysql_container
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 'root'
      MYSQL_DATABASE: 'ltweb'
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
